import 'package:flutter/material.dart';

class ContactWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Coming soon...", style: TextStyle(fontSize: 19.0, fontWeight: FontWeight.bold)),
    );
  }
}
